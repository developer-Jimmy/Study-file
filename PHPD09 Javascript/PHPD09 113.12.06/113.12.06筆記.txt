除以0會印出無限大
負數除以0會印出負數無限大
看console顏色是字串為黑色，數值為藍色

宣告陣列 放資料 拿資料

印出變數(陣列)，是從地址拿數值，因此地址中的值改變
前面在地址拿值的變數也會改變數值

陣列如果很多變數，可以加入屬性 ==> 屬性 + 數值

物件.屬性 ==> 目標值

(句點跟中括號都可以接續屬性)，看是要新增還是修改或取得屬性值

console.log(b++);(先顯示b在++)

== (值相等時true) ===(資料型態不相等時false)

getMonth (js讀取是0~11)
給人看的話要先+1

getDay的0是星期日 (js)

prompt 輸入資訊
alert 直接跳出訊息